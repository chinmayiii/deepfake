services:
  - type: web
    name: deepfake-detector
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web-app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
